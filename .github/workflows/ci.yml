name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check MSW worker
        run: pnpm run msw:check

      - name: Audit dependencies
        run: pnpm run audit:deps

      - name: Formatting
        run: pnpm run format:check

      - name: Validate config
        run: pnpm run validate

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Build (static site)
        run: pnpm run build

      - name: Check initial bundle size
        run: pnpm run check:bundle

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium firefox webkit

      - name: Tests (Playwright)
        run: pnpm test

      - name: Benchmark runner regression tests
        run: pnpm test:benchmark

      - name: Benchmark runner smoke test
        run: pnpm benchmark --suite benchmarks/example --accounts prof-demo,prof-ays --output artifacts/ci-matrix

      - name: Upload benchmark evidence
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-evidence
          path: artifacts
          retention-days: 14

      - name: Build and smoke-test container
        run: bash scripts/check-container.sh

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report
          retention-days: 7
